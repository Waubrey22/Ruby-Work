<%= form_for @genre do |genre| %>
  <!-- Form so user can enter items for new category -->
  <p><%= genre.label :title %> <%= genre.text_field :title %>
  <p><%= submit_tag "Create Category" %>
<% end %>